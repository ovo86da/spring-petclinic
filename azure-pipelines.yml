trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

pool:
  name: Default

variables:
  MAVEN_CACHE_FOLDER: '$(Pipeline.Workspace)/.m2/repository'
  MAVEN_OPTS: '-Dmaven.repo.local=$(MAVEN_CACHE_FOLDER)'

stages:
  - stage: build_and_sbom
    displayName: 'Build and generate CycloneDX SBOM'
    jobs:
      - job: build
        displayName: 'Build Spring Petclinic'
        steps:
          - checkout: self
            clean: true
            fetchDepth: 1
            persistCredentials: false

          - script: |
              echo "PATH=$PATH"
              echo "JAVA_HOME=$JAVA_HOME"
              which java || true
              java --version
              chmod +x mvnw
            displayName: 'Verify Java and make mvnw executable'

          - task: Cache@2
            displayName: 'Cache Maven repository'
            inputs:
              key: 'maven | "$(Agent.OS)" | pom.xml'
              restoreKeys: |
                maven | "$(Agent.OS)"
              path: '$(MAVEN_CACHE_FOLDER)'

          - script: |
              ./mvnw -B -ntp clean package -DskipTests
            displayName: 'Build JAR without tests'
            env:
              MAVEN_OPTS: $(MAVEN_OPTS)

          - script: |
              ./mvnw -B -ntp org.cyclonedx:cyclonedx-maven-plugin:makeAggregateBom -DskipTests
            displayName: 'Generate CycloneDX SBOM'
            env:
              MAVEN_OPTS: $(MAVEN_OPTS)

          - script: |
              ls -la target || true
            displayName: 'List target contents'

          - task: PublishBuildArtifacts@1
            displayName: 'Publish build artifacts'
            inputs:
              PathtoPublish: 'target'
              ArtifactName: 'petclinic-build'
              publishLocation: 'Container'